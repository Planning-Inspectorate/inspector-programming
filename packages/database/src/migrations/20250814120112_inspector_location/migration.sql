BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Inspector] ADD [latitude] DECIMAL(9,6),
[longitude] DECIMAL(9,6);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
