{% from "govuk/components/service-navigation/macro.njk" import govukServiceNavigation %}
{% from "ui/header/pins-header.njk" import pinsHeader %}

{% macro header(serviceName, isHomePage, isUnassignableCasesPage) %}
    {{ pinsHeader({isExternal: false}) }}

    {{ govukServiceNavigation({
        serviceName: serviceName,
        serviceUrl: "/",
        classes: 'pins-service-navigation',
        navigation: [
            {
                href: "/",
                text: "Home",
                active: isHomePage
            },
            {
                href: "/unassignable-cases",
                text: "Unassignable cases",
                active: isUnassignableCasesPage
            },
            {
                href: "/auth/signout",
                text: "Sign out"
            }
        ]
    }) }}
{% endmacro %}
